name: release-beta

on:
  pull_request:
    types: [closed]
    branches: [beta]

env:
  GODOT_VERSION: 4.5
  EXPORT_NAME: example
  PROJECT_PATH: . # NOTE: If your `project.godot` is at the repository root, set `PROJECT_PATH` to ".".

jobs:
  build:
    name: Build Godot Project
    uses: ./.github/workflows/build.yml
    if: startsWith(github.head_ref, 'alpha-feat')
  create-release:
    name: Create alpha release from prerelease
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'alpha-feat')
    needs: build
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Create Tag From Prerelease
        id: release_tag
        uses: mistermaxis/update-tag-action@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          prefix: v
          bump: none
          suffix: alpha
          replace_suffix: true
          new_suffix: alpha
      - name: Download Files
        uses: actions/download-artifact@v4
        with:
          path: build
          merge-multiple: true
      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          artifacts: build/*
          tag: ${{ steps.release_tag.outputs.updated_tag }}
          name: Release ${{ steps.release_tag.outputs.updated_tag }}
          make_latest: true
